# What did you learn from reading and commenting on this code?
I feel like I much better grasp the concepts of .map() .filter() and .reduce(), along with writing functions that utilize '=>'. I also feel much more comfortable using functions with multiple parameters after seeing them in practice more often.

# What was the most confusing part?
The most confusing part to me was the uniqueHobbies function. The syntax of '[...new Set(names)]' was confusing at first, but I came to realize it just takes the data from the names array and creates a new array that removes duplicate values. 